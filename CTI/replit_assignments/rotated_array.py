# Supposed sorted array A is rotated at some unknown pivot. Find the minumum element. Array will not contain duplicates. Do not use sort. Figure out how many times array was rotated 

# A[rotations] = min element
def min_of_rotated_array(A):
  rotations = 0
  # binary search  
  
# This is called a circularly sorted array
print(min_of_rotated_array([4,5,6,7,0,1,2])) #0